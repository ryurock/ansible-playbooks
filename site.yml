- hosts: all
  sudo: yes

- hosts: OSX
  roles:
    - { role: zsh/zshrc/common}
    - { role: git/gitconfig}
    - { role: python/pyenv/install}
    - { role: python/pyenv/python-build}
    - { role: homebrew/brew/cask}
